{
  "name": "tools-packages",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier —write ."
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "install": "^0.13.0",
    "npm": "^8.5.1",
    "prettier": "^2.5.1"
  }
}
